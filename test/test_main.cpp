#include <iostream>
#include "uAircraft.h"
#include "utest.h"
using namespace std;

int main()
{
    cout<<"start testing..."<<endl;
    //UnmannedAircraft ua;
    //ASSERT_TRUE(Position(0,0,0,N) == ua.gePosition());
    ASSERT_TRUE(0 == 0);
    return 0;
}
